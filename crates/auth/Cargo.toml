[package]
name = "auth"
version = "0.1.0"
edition = "2021"

[dependencies]
jsonwebtoken = { version = "10", features = ["rust_crypto"] }
serde = {version = "1.0", features = ["derive"] }
error-stack = "0.4"